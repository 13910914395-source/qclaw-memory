# 海南勘察招标日报 · 任务记录（2026-08-08 03:00）

## 目标
按定时任务要求，抓取两个平台最近 24 小时（2026-08-07 03:00 ~ 2026-08-08 03:00）发布的勘察/检测/测绘/岩土/地质灾害类公告，合并去重、智能识别真实勘察类项目，生成 PDF 日报 + 钉钉卡片摘要。

## 数据源与技术路径（关键，供复用）

### 1. 中国招标投标公共服务平台
- 门户 `www.cebpubservice.com` 首页为静态页，搜索入口在 `bulletin.cebpubservice.com`。
- **可用检索接口（HTML 结果页，无需登录）**：
  `https://bulletin.cebpubservice.com/xxfbcmses/search/{cat}.html`
  - `cat` ∈ `bulletin`(招标公告) / `change`(变更) / `result`(结果) / `candidate`(候选人) / `qualify`(资格预审)
  - 关键参数：`searchDate`、`dates`、`categoryId=88`、`word=<关键词二次URL编码>`、
    **`startcheckDate=YYYY-MM-DD&endcheckDate=YYYY-MM-DD`（真正生效的发布时间过滤）**、`page=N`（每页 20 条）
  - `dates` 参数实测不生效，必须用 `startcheckDate/endcheckDate`。
  - `word` 需 `quote(quote(kw))` 双重编码；平台为模糊分词匹配（搜"岩土"会命中"龙岩""花岗岩"），必须本地二次过滤。
- 详情页：`https://ctbpsp.com/#/bulletinDetail?uuid=<ID>`；后端 `https://ctbpsp.com/cutominfoapi/bulletin/detail?uuid=<ID>` 有 JS 反爬混淆，脚本无法直接解析正文。

### 2. 海南省政府采购网
- `www.ccgp-hainan.gov.cn` 301 跳 `ccgp-hainan.gov.cn`（**curl 必须去 www 或加 -L**）。
- Vue SPA（`gpcms-center-web`），配置在 `/gpcms-center-web/static/config.js`，API 基址 `/gpcms`。
- **可用接口**：`https://ccgp-hainan.gov.cn/gpcms/rest/web/v2/app/selectInfoMoreChannelForApp?pageNo=1&pageSize=500`
  - 返回 JSON，含 `title / budget / purchaser / noticeTime / noticeTypeName / openTenderCode / regionName / content`（正文 HTML 全文）。
  - **翻页参数无效**（pageNo/page/current 均不分页），靠加大 `pageSize` 覆盖（500 条可回溯至 5 天前）。
  - `info/selectInfoMoreChannel` 与 `info/selectInfoForIndex` 返回 4009，仅 App 端接口可用。
- 详情页 URL 规则：`https://ccgp-hainan.gov.cn/#/articleHn?type=article&id=<info id>`
- Python 请求需 `ssl._create_default_https_context = ssl._create_unverified_context`（本机缺根证书）。

## 抓取结果
- 国家平台 24h 内（2026-08-07~08）去重后 **417 条**；海南政采 24h 内 **130 条**；合计 547 条，跨平台无重复主体。
- 智能过滤后：核心勘察/测绘/岩土/地灾 **72 条**；工程质量与试验检测 **36 条**；海南本地相关 **12 条**。
- **海南真正可投标的勘察检测机会仅 2 条**：
  1. 澄迈县自然资源保护动态监测服务（竞争性磋商，362.45 万，测绘乙级+中小企业专项，截止 2026-08-20 09:00）
  2. 陵水县文罗物流基地及配套基础设施项目工程质量检测（公开招标，预算未公开，招标人陵水智京物流供应链有限公司）

## 关键结论
- 海南省政府采购网**不是**海南工程勘察类招标的主阵地；工程建设类勘察设计走「海南省公共资源交易服务平台」，当前口径存在系统性漏采，建议下一版增补该源。
- 澄迈项目硬门槛为**乙级及以上测绘资质 + 中小企业**，大型企业出局；且要求部署 ≥10 套固定机巢式无人机 + 集成化管理平台，属复合型需求。
- 检测类项目普遍以 **CMA 附表参数覆盖度**为废标高发点。

## 产出物
- PDF：`/Users/fasimac/.qclaw/workspace/海南勘察招标日报_2026-08-08.pdf`（11 页，495,425 字节，SHA256 `251dffbfc8e63c0b44dd28349c8b160e1700fb41a0ac9b6aa216162655930790`）
- Base64：`/Users/fasimac/.qclaw/workspace/海南勘察招标日报_2026-08-08.pdf.base64.txt`（660,569 字节，因体积过大未在对话中内联输出）
- 生成脚本：`/tmp/mkpdf.py`；抓取脚本：`/tmp/ceb_fetch.py`、`/tmp/hn_fetch.py`
- 字体：STHeiti Light/Medium（macOS 内置），reportlab 5.0.0，WPS/Acrobat 兼容
